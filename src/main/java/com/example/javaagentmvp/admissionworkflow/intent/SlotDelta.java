package com.example.javaagentmvp.admissionworkflow.intent;

/** Which slot the user changed in a multi-turn follow-up. */
public enum SlotDelta {
    NONE,
    SCORE,
    PROVINCE,
    SUBJECT_GROUP,
    YEAR,
    ADMISSION_TYPE
}
