<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.javaagentmvp.auth.persistence.mapper.AuthSessionMapper">

    <insert id="insert">
        INSERT INTO agent_ui.auth_session (
            id, user_id, jwt_jti, issued_at, last_active_at, expires_at
        )
        VALUES (
            #{id}, #{userId}, #{jwtJti}, #{issuedAt}, #{lastActiveAt}, #{expiresAt}
        )
    </insert>

    <select id="findById" resultType="com.example.javaagentmvp.auth.persistence.model.AuthSessionRecord">
        SELECT id,
               user_id,
               jwt_jti,
               issued_at,
               last_active_at,
               expires_at,
               revoked_at
        FROM agent_ui.auth_session
        WHERE id = #{id}
    </select>

    <update id="touchLastActive">
        UPDATE agent_ui.auth_session
        SET last_active_at = #{lastActiveAt}
        WHERE id = #{id}
          AND revoked_at IS NULL
    </update>

    <update id="revoke">
        UPDATE agent_ui.auth_session
        SET revoked_at = #{revokedAt}
        WHERE id = #{id}
          AND revoked_at IS NULL
    </update>

</mapper>
